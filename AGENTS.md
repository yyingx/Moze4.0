# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

## 概述

Python 脚本集，将支付宝/微信/云闪付账单转换为 Moze 4.0 导入格式。

## 快速开始

```bash
# 依赖安装
pip install pandas numpy openpyxl
pip install paddlepaddle==2.6.2 paddleocr==2.7.3 opencv-python  # OCR 需要

# 常用命令（推荐使用 Skill）
/convert                                   # 账单转换（自动检测最新版本）
/ocr                                       # 云闪付 OCR
/reimburse                                 # 差旅报销生成
/commit                                    # 规范化 commit（分析变更）

# 或直接运行脚本
python Moze4.0_Import_v11.87.py           # 支付宝/微信账单转换
python Moze4.0_账单备注同步_v0.1.py       # vivo 账单备注同步到微信/支付宝账单
python "Moze4.0_云闪付PaddleOCR 稳定版_.py"  # 云闪付 OCR
python Moze4.0报销_Enhanced.py             # 差旅报销生成
```

## 版本选择

| 版本       | 特性                                             | 推荐场景     |
| ---------- | ------------------------------------------------ | ------------ |
| **v11.87** | 固定餐饮规则边界整理，餐别触发词负责时间推导     | **日常使用** |
| v11.86     | 按收支区分拼多多返利，避免支出误归为返利         | 上一版       |
| v11.85     | 增加微信无备注转账收入提醒，并过滤需手动处理记录 | 旧版         |
| v11.84     | 停车费报销、退款提示、返利/收入映射修复          | 旧版         |
| v11.83     | 支持字典规则保留收入映射                         | 旧版         |
| v11.82     | 日常转换基础版                                   | 旧版         |

## Skill 命令

| 命令         | 功能       | 说明                              |
| ------------ | ---------- | --------------------------------- |
| `/convert`   | 账单转换   | 自动检测最新稳定版本              |
| `/ocr`       | 云闪付 OCR | PaddleOCR 识别截图                |
| `/reimburse` | 报销单生成 | 差旅/费用报销                     |
| `/commit`    | 规范化提交 | 分析变更，生成规范 commit message |

**推荐工作流：** `/commit` → 手动 push 或 VS Code 同步

## 核心数据结构

**⚠️ 修改前必读：**

- **`DATA_SOURCE`** - 关键词字典（不要随便改）
- **`INGREDIENT_PRIORITY`** - 匹配优先级顺序（顺序决定匹配结果）
- **`RAW_MAPPING_CONFIG`** - 子类别 → (记录类型, 主类别, 项目) 映射
- **`SUBCAT_KEYWORDS`** - 备注中的子类别关键词，当前 59 个输入关键词
- **`NAME_KEYWORDS`** - 备注中的名称关键词，当前 15 个输入关键词

**餐饮规则边界：**
- `DATA_SOURCE['MEAL']` 是餐别触发器，可包含固定餐饮地点、品牌、食堂和具体餐品词。
- `Moze Dict.xlsx` 负责商家清洗、商家+商品复合规则、描述和标签。
- 复合规则示例：`商家(old)=武汉工程大学` + `商品=武汉工程大学`，或 `商家(old)=湖北工业大学` + `商品=东苑`。
- 需要按时间推导餐别的字典规则，`子类别` 应留空，`项目` 填 `食`；同时确保对应地点/品牌/餐品能命中 `MEAL`。
- `商品` 列支持正则，但必须设置 `is_regex=1`；如需同时匹配东苑/西苑可写 `(?:东|西)苑`。

**修改禁忌：**
- 不要打乱 `INGREDIENT_PRIORITY` 顺序
- 不要删除 `RAW_MAPPING_CONFIG` 中已有的子类别
- 不要在 `exclude_if_match` 中引用不存在的 key
- 不要直接改旧版本脚本；需要行为变更时新建 `Moze4.0_Import_v11.XX.py`
- 新建版本脚本时必须同步更新 `__version__` 和 `__updated__`
- 不要提交支付宝/微信源账单、Moze 导出 CSV、截图账单等私人数据

## 手动添加行语法

在 Excel 备注列使用 `关键词+描述` 格式：

### 子类别关键词 (SUBCAT_KEYWORDS)

| 备注         | 子类别      | 名称 | 描述   |
| ------------ | ----------- | ---- | ------ |
| `日用`       | 日常用品    | -    | -      |
| `日用洗衣液` | 日常用品    | -    | 洗衣液 |
| `药品感冒药` | 药品        | -    | 感冒药 |
| `图书Python` | 图书        | -    | Python |
| `午餐`       | 午餐        | -    | -      |
| `晚餐麦当劳` | 晚餐        | -    | 麦当劳 |

早餐、午餐、晚餐、夜宵为手动指定；自动餐别推导由 `DATA_SOURCE['MEAL']` 和交易时间控制。

### 名称关键词 (NAME_KEYWORDS)

| 备注         | 子类别   | 名称       | 描述   |
| ------------ | -------- | ---------- | ------ |
| `饮料`       | 饮料水果 | 饮料       | -      |
| `饮料可乐`   | 饮料水果 | 饮料       | 可乐   |
| `加油中石化` | 加油充电 | 加油       | 中石化 |
| `水果苹果`   | 饮料水果 | 水果       | 苹果   |
| `鸡蛋`       | 食材     | 蛋及蛋制品 | -      |
| `鸡蛋土鸡蛋` | 食材     | 蛋及蛋制品 | 土鸡蛋 |

### 应收应付关键词

| 备注                  | 子类别 | 名称 | 对象   | 描述     |
| --------------------- | ------ | ---- | ------ | -------- |
| `借出谢辉`            | 借出   | -    | 谢辉   | -        |
| `借入谢辉`            | 借入   | -    | 谢辉   | -        |
| `借出龚本强.华风螺丝` | 借出   | -    | 龚本强 | 华风螺丝 |
| `代付张三`            | 代付   | -    | 张三   | -        |
| `押金`                | 押金   | -    | -      | -        |


“外勤”不在作为商家  ，作为标签

### 报销关键词

| 备注                   | 子类别 | 名称     | 对象   | 描述         | 标签      |
| ---------------------- | ------ | -------- | ------ | ------------ | --------- |
| `住宿费`               | 报账   | 住宿费   | 天之逸 | -            | #差旅报销 |
| `住宿费汉庭`           | 报账   | 住宿费   | 天之逸 | 汉庭         | #差旅报销 |
| `材料费角铁钉子等`     | 报账   | 材料费   | 天之逸 | 角铁钉子等   | #费用报销 |
| `物流运输货拉拉高速费` | 报账   | 物流运输 | 天之逸 | 货拉拉高速费 | #费用报销 |

**差旅报销：** 车船费、住宿费、住宿补贴、交通补贴、餐费补贴
**费用报销：** 材料费、燃油费、交通费、过路费、租赁费、叉车费、停车费、印刷服务、物流运输、市内交通、生活用品、人工劳务费、代付货款、招待费、汽车费用

### 关键词列表

**SUBCAT_KEYWORDS（59 个输入关键词 → 子类别）：**

| 主类别 | 输入关键词                                                             | 子类别   |
| ------ | ---------------------------------------------------------------------- | -------- |
| 饮食   | 早餐、午餐、晚餐、夜宵                                                 | 同名     |
| 饮食   | 食材、饮料水果、纯净水、零食                                           | 同名     |
| 购物   | 日用                                                                   | 日常用品 |
| 购物   | 服饰、服饰鞋包                                                         | 服饰鞋包 |
| 购物   | 数码、数码电器                                                         | 数码电器 |
| 购物   | 家具、家具家纺                                                         | 家具家纺 |
| 购物   | 大件                                                                   | 大件     |
| 交通   | 加油充电、公共交通、火车、机票、出租车                                 | 同名     |
| 居家   | 快递、快递邮政                                                         | 快递邮政 |
| 居家   | 房租、水费、电费、物业费、宽带费、理发、电话费                         | 同名     |
| 虚拟   | App、Software、软件、订阅、影音                                        | 同名     |
| 娱乐   | 电影、聚会                                                             | 同名     |
| 娱乐   | 旅游                                                                   | 旅游度假 |
| 娱乐   | 游戏                                                                   | 网游电玩 |
| 医疗   | 药品、门诊、体检、保健用品                                             | 同名     |
| 学习   | 图书、教材、探索、文具                                                 | 同名     |
| 个人   | 保险                                                                   | 同名     |
| 个人   | 礼金、红包                                                             | 礼金红包 |
| 收入   | 薪资、福利补贴、年终奖、收红包、利息收入、投资盈利、二手折旧、其他收入、返利回馈 | 同名     |

自动餐别推导不靠备注写 `正餐`，而是命中 `DATA_SOURCE['MEAL']` 后按交易时间推导早餐/午餐/晚餐/夜宵。

**NAME_KEYWORDS（15 个输入关键词 → 子类别 + 名称）：**

| 输入关键词                                   | 子类别   | 名称       |
| -------------------------------------------- | -------- | ---------- |
| 水果、饮料                                   | 饮料水果 | 水果/饮料  |
| 加油、充电                                   | 加油充电 | 加油/充电  |
| 蔬菜、猪肉、牛羊肉、禽肉、豆制品、熟食、大米 | 食材     | 同名       |
| 海鲜水产                                     | 食材     | 海鲜水产   |
| 鸡蛋                                         | 食材     | 蛋及蛋制品 |
| 节点                                         | 虚拟其他 | 节点       |
| 厨房用品                                     | 日常用品 | 厨房用品   |

## 关键文件

- **`Moze Dict.xlsx`** - 商家规则字典（必需）
- **`Moze4.0_Import/`** - 输出目录

## vivo 账单备注同步

脚本：`Moze4.0_账单备注同步_v0.1.py`

用途：从 vivo 账单中提取有效手写备注，同步到同日期区间的微信/支付宝原始账单备注列。

```bash
python Moze4.0_账单备注同步_v0.1.py
```

运行后会弹窗选择 vivo 账单，例如 `2026.06.05-2026.06.07.xlsx`，脚本根据文件名日期区间自动识别：

- `微信支付账单流水文件(YYYYMMDD-YYYYMMDD)_*.xlsx`
- `支付宝交易明细(YYYYMMDD-YYYYMMDD).csv`

同步规则：

- 按金额相等、交易时间相近匹配记录。
- 默认只填充微信/支付宝账单中空备注或 `/`，不覆盖已有备注。
- 如需强制覆盖已有备注，显式添加 `--overwrite-existing`。
- 备注提取优先使用 `|` 或 `｜` 后的内容；无分隔符时，自动剥离交易对方、商品说明和脱敏括号，剩余内容作为有效备注。

常用检查：

```bash
python Moze4.0_账单备注同步_v0.1.py --dry-run
python Moze4.0_账单备注同步_v0.1.py --version
```

## 当前导入规则要点

- 支出默认导入；收入只导入明确可分类记录，避免把退款、内部流转误作收入。
- `已全额退款` 和 `交易关闭` 不导入。
- 退款行只提示不导入：
  - 支付宝按订单号前 8 位推断原交易日期，`0-1` 天视为快速退款，`>=2` 天视为退货/部分退款。
  - 微信按状态识别：`已退款¥x` 作为需要手动处理的退款收入；`已退款(¥x)` 的原支出行不单独提示。
  - 退款提示使用 CSV 格式输出，并带 `来源` 列。
- 无备注微信转账收入只提示不导入：
  - 条件：微信、收入、交易类型为转账、备注为空、商品文本包含“微信转账”。
  - 排除已定义的内部转账对象，如肖恩、工商银行(9579)、上海雪球数智科技有限公司。
  - 有业务备注时备注规则优先，不进入该提醒。
- `停车费` 与 `叉车费` 同属公司费用报销；备注写 `停车费...` 时按 `报账/#费用报销/对象=天之逸` 处理。
- 拼多多等返利广告描述会清空，避免广告文案进入 Moze 描述。
- `PAUSE_ON_EXIT = False`，脚本运行结束默认不等待 Enter。

## 版本管理

**命名规则：** `Moze4.0_Import_v11.XX.py`

新建版本时同步更新文件头：

```python
__version__ = '11.XX'
__updated__ = 'YYYY-MM-DD'
```

**Commit 规范：**
```text
新增 vXX.XX: 功能描述
优化 vXX.XX: 改进内容
修复 vXX.XX: 问题描述
```

账单源文件和导出结果已通过 `.gitignore` 忽略，历史清理后不要重新加入。

## 报销生成器配置

```python
CFG = {
    'MEAL': 40,      # 餐补（元/天）
    'TRANS': 20,     # 交补（元/天）
    'HOTEL': 140,    # 住宿标准（元/天）
}
```
日期格式：`yymmdd` 或 `yymmdd-yymmdd`

## 云闪付 OCR 配置

```python
DEFAULT_YEAR = 2026
MY_NAME = "应翔"
ACCOUNT_MAP = {...}  # 银行卡尾号映射
```

## 调试

```python
# 启用详细日志
logging.basicConfig(level=logging.DEBUG)

# 测试单个文件
file_path = Path("测试文件.csv")
```
